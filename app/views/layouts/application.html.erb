<!DOCTYPE html>
<html>
<head>
  <title>Final3901</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <style type="text/css">
.headerDiv {display: inline;
			font-size: 40px;}
.headerDivRight {font-size: 20px;}
  </style>
</head>
<body>

	<div id="header">
		<div class="headerDiv"><%= link_to image_tag("buy sell icon.jpg"), root_url %></div>
		<div class="headerDiv" style="text-align:center">TADS</div>
		<div class="headerDivRight" style="float: right;">
		<% if session[:user_id] %>
		<!--user id:--> <%#= session[:user_id] %>
		<div><%= User.find(session[:user_id]).username %></div>
		<div><%= button_to 'Logout', {:controller => "sessions", :action => "destroy", :id => session[:user_id]}, method: :delete %> </div>
	</div>
<!--		<div class="headerDiv">Notifications</div> -->
		<% end %>
	</div>

<%= yield %>

</body>
</html>
